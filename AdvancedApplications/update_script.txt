ALTER TABLE EMPLOYEES 
ADD (EMP_IMAGE BLOB );

ALTER TABLE EMPLOYEES 
ADD (EMP_DOC VARCHAR2(200) );


CREATE TABLE MAIN_SYSTEM_MENU 
(
  MENU_ID NUMBER NOT NULL 
, MENU_DESCRIPTION VARCHAR2(50) 
, MENU_ACTIVE VARCHAR2(20) 
, CONSTRAINT MAIN_SYSTEM_MENU_PK PRIMARY KEY 
  (
    MENU_ID 
  )
  ENABLE 
);


CREATE TABLE SUB_MENU 
(
  SUB_MENU_ID NUMBER NOT NULL 
, PARENT_MENU_ID NUMBER 
, SUB_MENU_DESCRIPTION VARCHAR2(100) 
, SUB_MENU_ACTION VARCHAR2(50) 
, SUB_MENU_ACTIVE VARCHAR2(20) 
, CONSTRAINT SUB_MENU_PK PRIMARY KEY 
  (
    SUB_MENU_ID 
  )
  ENABLE 
);

